export class Goal {
    constructor(inName, inEncounterCount, inReward, inState) {
        this.name = inName;
        this.encounterCount = inEncounterCount;
        this.reward = inReward;
        this.state = inState;
    }
}